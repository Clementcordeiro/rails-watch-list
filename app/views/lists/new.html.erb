<h1>New List</h1>
<%= simple_form_for [@list] do |l| %>
  <%= l.input :name %>
  <%= l.input :photo, as: :file %>
  <%= l.submit "Submit list", class: "btn btn-primary" %>
<% end %>

<%= link_to "See all lists", lists_path(@list)%>